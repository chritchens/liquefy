/// `http` defines the types and functions for the HTTP API of the Liquidity Network hub.
pub mod http;

/// `ws` defines the types and functions for the WebSocket API of the Liquid Network hub.
pub mod ws;
